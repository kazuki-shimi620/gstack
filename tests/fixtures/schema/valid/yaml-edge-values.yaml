name: unicode_metadata
model:
  displayName: Unicode Metadata
database:
  primaryKey: id
  columns:
    id:
      type: integer
metadata:
  japanese: 日本語
  integer: 42
  decimal: 1.5
  boolean: true
  nullValue: null
  explicitlyTagged: !!str 123
