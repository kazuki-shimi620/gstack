# Generator Specification

> Documentation index: [`../README.md`](../README.md)

> Version: 0.1.0 (Draft)

---

# 1. Purpose

Generatorは、Schemaを元にアプリケーションに必要な成果物（Artifacts）を生成するコンポーネントである。

gstackではGeneratorを単なるコードジェネレータではなく、

**Schemaからアプリケーション全体を構築するBuild Engine**

として定義する。

GeneratorはProviderへ依存しない。

---

# 2. Design Philosophy

Generatorは以下の原則に従う。

- Application Modelをアプリケーション定義の唯一の入力とする
- Providerを意識しない
- 生成物は再生成可能である
- 手動編集領域を破壊しない
- AIが理解しやすい構造を生成する
- 同じSchemaから常に同じ成果物を生成する

---

# 3. Architecture

```text
Application Model
    │
    ▼
Generator Engine
    │
    ├── Type Generator
    ├── API Generator
    ├── UI Generator
    ├── Validation Generator
    ├── Documentation Generator
    ├── AI Generator
    └── Template Engine
            │
            ▼
Generated Artifacts
```

---

# 4. Generator Flow

```text
Receive Validated Application Model

↓

Load Generator Config and Templates

↓

Resolve Dependencies

↓

Generate Artifacts

↓

Write Files

↓

Complete
```

---

# 5. Input

Generatorが入力として扱うもの

- Application Model
- Generator Config
- Template
- Plugin

GeneratorはDatabaseやProviderの状態を参照しない。

---

# 6. Output

Generatorは以下を生成する。

Backend

- API
- Routing
- DTO
- Validation
- Service

Frontend

- React Component
- Form
- Table
- List
- Detail

Common

- TypeScript
- OpenAPI
- Documentation
- AI Documentation

---

# 7. Generated Directory

例

```text
generated/

├── api/
├── frontend/
├── types/
├── validation/
├── docs/
├── ai/
└── openapi/
```

Generated配下はすべて再生成可能とする。

---

# 8. Manual Code

手動編集するコードはGeneratedから分離する。

例

```text
generated/

custom/

app/
```

Generatorはcustom配下を変更しない。

---

# 9. Type Generator

Schemaから型を生成する。

MVPの出力path、命名、Field type mapping、optional property規則は`DECISIONS.md` D-027を規範とする。Type GeneratorはApplication ModelからArtifact inputを返し、Artifact checksumやManifest作成は共通Generator Engineが担当する。

例

Application Model

```yaml
name: users

database:
  columns:
    id:
      type: uuid

    name:
      type: string
```

↓

Generated

```typescript
export interface User {
  id: string;

  name: string;
}
```

---

# 10. Validation Generator

Validationコードを生成する。

MVPでは外部libraryに依存しない構造化validatorを生成する。検証対象、result、coercion禁止、出力pathは`DECISIONS.md` D-028を規範とする。

入力

Application Model

↓

出力

- Validator
- Request Validation
- Form Validation

---

# 11. API Generator

API Layerを生成する。

生成対象

- CRUD
- Routing
- DTO
- Request
- Response

GeneratorはBusiness Logicを生成しない。

---

# 12. UI Generator

UI Generatorは基本画面を生成する。

生成対象

- List
- Detail
- Form
- Search
- Filter
- Pagination

UIデザインはTemplateに依存する。

---

# 13. Documentation Generator

以下を生成する。

MVPのModel reference documentは`DECISIONS.md` D-030を規範とし、Application Modelの構造だけを決定的なMarkdown Artifactへ変換する。

- API Documentation
- OpenAPI
- Entity一覧
- ER情報
- Route一覧

---

# 14. AI Documentation Generator

AI向けドキュメントを生成する。

MVPではGenerator所有領域の`PROJECT_CONTEXT.md`と限定scopeの`AGENTS.md`だけを生成する。手動Agent ruleを上書きせず、secretやruntime stateを含めない契約は`DECISIONS.md` D-031を規範とする。

例

- AGENTS.md
- Project Context
- Database Summary
- API Summary

AIがProject全体を理解しやすい情報を出力する。

---

# 15. OpenAPI Generator

SchemaからOpenAPIを生成する。

MVPの公開operation、path、component schema、型mappingは`DECISIONS.md` D-029を規範とする。出力はProvider／runtime framework非依存のOpenAPI 3.1 JSON Artifactとする。

例

```yaml
/users

GET

POST

PUT

DELETE
```

---

# 16. Template Engine

GeneratorはTemplateを利用してコード生成する。

例

```text
templates/

react/

fastify/

openapi/

typescript/
```

Templateを差し替えることで生成物を変更できる。

---

# 17. Plugin

PluginはGeneratorへ新しい生成対象を追加できる。

例

```text
PDF Generator

Markdown Generator

ER Diagram Generator
```

---

# 18. Generator Pipeline

```text
Schema

↓

Generators

↓

Template Engine

↓

Generated Files
```

Generator同士は独立して動作する。

---

# 19. Generated Files

Generated Fileは手動編集対象ではない。

ファイル先頭へ自動生成コメントを付与する。

例

```text
This file was generated by gstack.

DO NOT EDIT.
```

---

# 20. Merge Policy

Generatorは既存Generated Fileを更新できる。

appおよびcustom配下は更新しない。

Generatorは手動変更を保持する責務を持たない。

GeneratorはGenerated Artifact Manifestを保持し、Manifestに記録された`generated/`配下の古い生成物だけを削除できる。

MVPのArtifact／Manifest／再生成Planの厳密な契約は`DECISIONS.md` D-026を規範とする。Generator EngineはpureなPlanまでを所有し、filesystem変更はWriter adapterへ分離する。

---

# 21. Regeneration

Generatorは何度でも実行できる。

```bash
gstack generate
```

は常に最新Schemaから再生成する。

---

# 22. Incremental Generation

将来的には差分生成をサポートする。

例

```text
users.yaml変更

↓

User.tsのみ更新
```

初期バージョンでは全体生成でもよい。

---

# 23. Dependency Rule

Generatorは以下のみ参照する。

- Application Model
- Template
- Config

参照してはいけない。

- Database
- Provider
- Runtime

---

# 24. Error Handling

Generatorは以下のErrorを返す。

- SchemaError
- TemplateError
- ValidationError
- GeneratorError

---

# 25. Generator Configuration

例

```yaml
generator:
  language: typescript

  frontend: react

  backend: appscript

  openapi: true
```

Generator ConfigはProject単位で管理する。

MVPのbuilt-in producer有効化とpure orchestrationは`DECISIONS.md` D-032を規範とする。Project Config Loaderへの永続形式追加は、filesystem Writer／CLI統合と同じPhaseで行う。

---

# 26. Template Override

Project独自Templateを利用できる。

例

```text
templates/

custom/
```

標準Templateより優先される。

---

# 27. Capability

GeneratorはCapability単位で構成される。

例

- Type Generator
- API Generator
- UI Generator
- Validation Generator
- AI Generator

必要なGeneratorのみ有効化できる。

---

# 28. Security

Generatorは以下を生成してはならない。

- Token
- Password
- Secret
- Credential

環境変数のみ参照する。

---

# 29. AI First

GeneratorはAIが理解しやすい成果物を生成する。

例

- 命名規則を統一する
- コメントを生成する
- OpenAPIを生成する
- AI Contextを生成する

---

# 30. Future Extensions

将来的に追加するGenerator

- Flutter
- Vue
- Angular
- GraphQL
- SDK
- Terraform
- ER Diagram
- Test Code
- Storybook
- Markdown

---

# 31. Out of Scope

初期バージョンでは対象外

- AIによる画面デザイン生成
- Business Logic生成
- Provider固有コード生成
- Database Migration生成
- Runtimeコード生成

---

# 32. Relationship with Other Documents

## Accepted MVP Contract

`DECISIONS.md` D-009 and D-010 resolve earlier draft ambiguity: Generator consumes Application Model, Generator Config, and Template—not raw Schema/AST or Provider state. Generator owns only `generated/`, tracks outputs in a Generated Artifact Manifest, and may delete only stale paths recorded in that manifest.

| Document        | Purpose           |
| --------------- | ----------------- |
| ARCHITECTURE.md | 全体構造          |
| CLI.md          | Generator CLI仕様 |
| REQUIREMENTS.md | 要件              |
| SCHEMA.md       | Generator入力仕様 |
| PROVIDER.md     | Provider仕様      |
| MIGRATION.md    | Migration仕様     |
| DEVELOPER.md    | Generator内部実装 |
| ROADMAP.md      | Generator拡張計画 |
